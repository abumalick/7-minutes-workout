node_modules
.DS_Store
dist
dist-ssr
*.local
.svelte-kit
/build

# local scratch (not committed)
.tmp/

# per-task git worktrees (see "Dev workflow" in CLAUDE.md)
.worktrees/

# per-session autopilot decision logs (local record, not tracked)
ai/autopilot/
